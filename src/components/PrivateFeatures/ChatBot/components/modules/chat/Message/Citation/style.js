import styled from "styled-components";

export default styled.div`
  .citation-line {
    display: flex;
    gap: 0.75em;
    align-items: center;
    margin-right: 2em;
    padding-block: 2em;
    color: var(--chatbot-primary);
  }
`;
